<%= form_for(@fight, url: monster_fights_path(@fight.challenger_id)) do |f| %>
    <%=f.label "Fight Title"%>
    <%=f.text_field(:title)%>
    <%= f.collection_select :defender_id, @monsters, :id, :name %>
    <%=f.submit%>
<%end%>